---
'@backstage-community/plugin-quay': minor
---

Migrated the Quay plugin and dev app from Material UI to Backstage UI (BUI). Replaced `@material-ui/*` with `@backstage/ui` components and `@remixicon/react` icons. `@backstage/ui` CSS import is required for consumers. No breaking API changes to plugin exports.
