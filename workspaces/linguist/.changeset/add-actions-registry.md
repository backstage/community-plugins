---
'@backstage-community/plugin-linguist-backend': minor
---

Registered Actions Registry actions for programmatic access to Linguist's language detection. Two actions are now available: `linguist:get-entity-languages` returns the language breakdown for a catalog entity, and `linguist:process-entities` triggers processing of pending and stale entities.
