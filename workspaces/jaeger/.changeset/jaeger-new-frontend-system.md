---
'@backstage-community/plugin-jaeger': minor
---

Added support for the new frontend system. The plugin can now be installed via its `alpha` export, registering the Jaeger traces entity content and its API. Legacy exports remain available from the main entry point.
