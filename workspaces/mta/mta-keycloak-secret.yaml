apiVersion: v1
kind: Secret
metadata:
  name: mta-keycloak-secret
  namespace: local-backstage-mta-7-3
type: Opaque
stringData:
  # IMPORTANT: Replace these values with the actual client credentials from your Keycloak instance
  # To get these values:
  # 1. Access your Keycloak admin console at: https://tackle-konveyor-tackle.ian2-3626522b15eedb880d7b99992e225c1b-0000.us-east.containers.appdomain.cloud/auth/
  # 2. Navigate to the 'tackle' realm (or 'mta' realm depending on your setup)
  # 3. Go to Clients -> Create a new client or use existing 'backstage-provider'
  # 4. Ensure the client has the following settings:
  #    - Client Protocol: openid-connect
  #    - Access Type: confidential
  #    - Service Accounts Enabled: ON
  #    - Valid Redirect URIs: https://backstage-developer-hub-local-backstage-mta-7-3.ibb-3626522b15eedb880d7b99992e225c1b-0000.us-east.containers.appdomain.cloud/*
  MTA_KEYCLOAK_CLIENT_ID: 'backstage-provider'
  MTA_KEYCLOAK_CLIENT_SECRET: 'REPLACE-WITH-ACTUAL-SECRET-FROM-KEYCLOAK'
  MTA_KEYCLOAK_REALM: 'tackle'  # or 'mta' depending on your setup
