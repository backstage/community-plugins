# Persisted feature flags

This workspace provides a plugin for _persisted_ feature flags (saved in the database). Look at the [README.md for the plugin](plugins/persisted-feature-flags/README.md) for more information.
