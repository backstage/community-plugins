---
'@backstage-community/plugin-topology': minor
---

Added support for the New Frontend System (NFS), including alpha exports with `EntityContentBlueprint` and `TranslationBlueprint`, a new NFS-compatible dev entry, and a new `app` package. The existing app has been renamed to `app-legacy`.
