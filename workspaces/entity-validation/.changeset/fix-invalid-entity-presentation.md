---
'@backstage-community/plugin-entity-validation': patch
---

Fixed invalid entity rendering in the entity validation UI so missing `kind` or empty `metadata` values now show validation results instead of crashing the page.
