app:
  title: Copilot Example App
  baseUrl: http://localhost:3000

  # Enable all packages by default, this will discover packages from packages/app/package.json
  packages: all

  extensions:
    # Configure the catalog index page to be the root page, this is normally mounted on /catalog
    - page:catalog:
        config:
          path: /

organization:
  name: Copilot Example

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  baseUrl: http://localhost:7007
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection:
      directory: '../../database/'
    useNullAsDefault: true

  # see https://backstage.io/docs/ai/mcp-actions#actions-configuration for more details
  actions:
    pluginSources:
      - auth
      - catalog
      - scaffolder
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir
techdocs:
  builder: 'local'
  generator:
    runIn: 'local'
  publisher:
    type: 'local'

auth:
  providers:
    # The guest user's entity ref is overridden here (instead of the default
    # `user:default/guest`) so that its `name` part — `your-github-username`
    # — matches a real GitHub login with ingested Copilot data. This lets you
    # exercise the individual "my metrics" plugin locally as the guest user,
    # since the default CopilotUserResolver resolves the caller's GitHub
    # login directly from this entity ref's name. Replace
    # `your-github-username` with your own GitHub username (or any login
    # that has ingested data) when testing.
    guest:
      userEntityRef: user:default/your-github-username

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location]
  locations:
    # Local example data, file locations are relative to the backend process, typically `packages/backend`
    - type: file
      target: ../../examples/entities.yaml

    # Local example organizational data
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

# integrations:
#   github:
#     - host: github.com
#       token: ${GITHUB_TOKEN}

integrations:
  github:
    - host: github.com
      apps:
        - appId: ${AUTH_GITHUB_APP_ID}
          clientId: ${AUTH_GITHUB_CLIENT_ID}
          clientSecret: ${AUTH_GITHUB_CLIENT_SECRET}
          webhookSecret: not_required
          privateKey: ${AUTH_GITHUB_PRIVATE_KEY}

permission:
  # setting this to `false` will disable permissions
  enabled: true

copilot:
  host: github.com
  # enterprise: ${ENTERPRISE}
  organization: ${ORGANIZATION}
  backfillFromDate: '2026-08-01'
  backfillDelayMs: 300
  ingestTeams: true
  showUserMetrics: true
  showLegacyView: false
  defaultView: enterprise
  schedule:
    frequency:
      minutes: 15
    timeout:
      minutes: 30
    initialDelay:
      seconds: 5
