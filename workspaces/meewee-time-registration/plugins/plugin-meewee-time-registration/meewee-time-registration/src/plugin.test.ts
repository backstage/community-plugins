import { meeweeTimeRegistrationPlugin } from './plugin';

describe('meewee-time-registration', () => {
  it('should export plugin', () => {
    expect(meeweeTimeRegistrationPlugin).toBeDefined();
  });
});
