---
'@backstage-community/plugin-rbac': patch
---

Add tests covering the RBAC page as the New Frontend System declares it: its title, route path, the route it resolves links against, and what it renders, plus that the translations ship as a module on the app plugin.
