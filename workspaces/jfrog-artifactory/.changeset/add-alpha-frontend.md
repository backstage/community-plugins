---
'@backstage-community/plugin-jfrog-artifactory': minor
---

Added alpha frontend support with blueprints for the new Backstage frontend system. The plugin now exports an `EntityContentBlueprint` and `ApiBlueprint` via the `/alpha` entry point.
