# Checkmarx Shared Common Package

Shared package with:

- summary and counter types
- catalog annotation constants
- helper functions such as `isCheckmarxAvailable` and `useProjectInfo`

This package is consumed by both the frontend plugin and the backend plugin.
