---
'@backstage-community/plugin-entity-feedback': minor
---

Updated entity feedback controls, dialogs, and tables to use Backstage UI instead of Material UI. To upgrade, use React 18 and React Router 6.30.2 or later, install `@backstage/ui` in the app, and import `@backstage/ui/css/styles.css` in the app entry point.
