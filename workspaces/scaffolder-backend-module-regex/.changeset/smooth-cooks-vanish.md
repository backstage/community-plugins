---
'@backstage-community/plugin-scaffolder-backend-module-regex': minor
---

Add functionality to apply regex search to a property on an array of objects and have the results appended to each object where values are found
