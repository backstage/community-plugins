---
'@backstage-community/plugin-todo': major
---

**BREAKING** Migrated the `@backstage/core-components` `Table` component to Backstage UI's `Table` component. Adopters need `@backstage/ui` styles available in the app (included by default since Backstage `1.41.0`).
