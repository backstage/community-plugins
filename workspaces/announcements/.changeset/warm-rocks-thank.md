---
'@backstage-community/plugin-announcements': patch
---

The announcement form has been rewritten with `@backstage/ui` where possible and now opens in a dialog for users on the new frontend system. This change does not affect users on the existing frontend system.
