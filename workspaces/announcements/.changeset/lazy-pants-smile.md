---
'@backstage-community/plugin-announcements': patch
---

Creating and editing an announcement will now render within a `@backstage/ui` Dialog component, matching the experience when creating categories and tags.
