---
'@backstage-community/plugin-announcements': patch
---

The core announcements page end users visit has been rewritten with `@backstage/ui` for users on the new frontend system. Users on the existing frontend system will not see any changes.
