## API Report File for "@backstage-community/plugin-techdocs-editor-node"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import { ExtensionPoint } from '@backstage/backend-plugin-api';

// @public
export type OpenPrOptions = {
  repoUrl: string;
  headBranch: string;
  baseBranch: string;
  title: string;
  description?: string;
  files: Map<string, string | null>;
  commitMessage: string;
  authorName: string;
  authorEmail: string;
  draft?: boolean;
  reviewers?: string[];
};

// @public
export type OpenPrResult = {
  url: string;
  number: number;
};

// @public
export const techdocsEditorVcsProviderExtensionPoint: ExtensionPoint<VcsProviderExtensionPoint>;

// @public
export type VcsFileResult = {
  content: string;
  etag: string;
};

// @public
export interface VcsProvider {
  canHandle(repoUrl: string): boolean;
  getDefaultBranch(repoUrl: string): Promise<string>;
  readonly id: string;
  listFiles(opts: {
    repoUrl: string;
    ref: string;
    dirPath: string;
  }): Promise<string[]>;
  openPullRequest(opts: OpenPrOptions): Promise<OpenPrResult>;
  readFile(opts: {
    repoUrl: string;
    ref: string;
    filePath: string;
  }): Promise<VcsFileResult>;
}

// @public
export interface VcsProviderExtensionPoint {
  // (undocumented)
  addProvider(provider: VcsProvider): void;
}
```
