---
'@backstage-community/plugin-tech-insights-maturity': patch
---

Added `techInsights.maturity` configuration for customizing the maturity feature title, help link, and help tooltip across the plugin UI. Components additionally accept a `title` prop when a single instance needs a different title. Entity links now follow the registered scorecard route when its path is customized.
