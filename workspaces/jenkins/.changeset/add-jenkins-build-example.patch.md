---
'@backstage-community/plugin-scaffolder-backend-module-jenkins': patch
---

Add a scaffolder example for the `jenkins:job:build` action so it appears in installed actions and users can see sample input usage.
